# CHANGELOG

همهٔ تغییرات مهم، کشفیات فنی و دیباگ‌های این پروژه در همین فایل ثبت می‌شود.
قالب الهام‌گرفته از [Keep a Changelog](https://keepachangelog.com/) است و نسخه‌ها بر پایهٔ
نسخهٔ k6 مرجعِ تست‌شده (`k6 v1.2.1`) ثبت می‌شوند.

---

## [Unreleased] — ۱۴۰۵/۰۷/۰۱ (2026-09-23) — دور دوم: یکپارچه‌سازی و تست واقعی سرویس gesture

### افزوده (Added)

- پیاده‌سازی اسکریپت تست دود واقعی `test-on-fara/smoke-test.js` با بهره‌گیری مستقیم از فریمورک `k6-module.checks`:
  - **پیکربندی سناریو**: سناریوی `gesture_smoke` با مجری `constant-vus` (پیش‌فرض ۵ کاربر مجازی بدون sleep جهت اعمال بار پیوسته).
  - **معیارها و آستانه‌ها (Thresholds)**:
    - آستانهٔ کل و تگ‌دار زمان پاسخ: `http_req_duration` با معیار `p(95) < 3000ms`.
    - آستانهٔ خطای پروتکل: `http_req_failed` با معیار `rate < 0.01` (نرخ خطای زیر ۱٪).
    - آستانه‌های تفکیک‌شده با `taggedSla` روی اندپوینت `endpoint:gestureRecognition` و انواع چک `type:availability` و `type:contract`.
    - گاردهای ضد سبز شدن کاذب با `sampleGuard()` و `guardThreshold({ endpoint: 'gestureRecognition' })`.
  - **اعتبارسنجی سلامت و لایه شبکه (Health Checks)**:
    - بررسی وضعیت کد ۲۰۰ HTTP، دریافت پاسخ و عدم خطای ارتباط شبکه با `healthChecks` و شدت بحرانی (`SEVERITY.critical`).
  - **اعتبارسنجی قرارداد پاسخ (Contract Checks)**:
    - انطباق ساختار خروجی API بر اساس نمونه‌های واقعی (`api response data.txt`):
      - وجود فیلدهای `status`, `data.message`, `data.result`, `meta.request_id`
      - نوع‌داده‌های `boolean` (برای `verified`)، `number` (برای `head_pose`)، `string` و آرایه غیرخالی `predicted_classes`
      - مقادیر موردانتظار بیزینس (`status: 200` و `data.message: "Success"`).
  - **سیستم گزارش‌گیری دوگانه و بایگانی (`handleSummary`)**:
    - چاپ خروجی استاندارد و ساختاریافتهٔ k6 با استفاده از `textSummary`.
    - محاسبهٔ توان عملیاتی زنده بر حسب درخواست بر دقیقه (RPM) و وضعیت معیارهای دوگانه.
    - ذخیره‌سازی خودکار گزارش متنی بدون کدهای رنگی ترمینال در پوشهٔ `test-on-fara/reports/` همراه با برچسب زمانی (`smoke-test-summary_YYYY-MM-DD_HH-mm.txt`).
- ایجاد پوشهٔ `test-on-fara/reports/` جهت نگهداری و آرشیو گزارش‌های متنی تست‌ها.

### رفع باگ (Fixed)

- **`base-stress-test.js`: رفع شکست بی‌صدای `handleSummary`.**
  - در نسخهٔ k6 v1.x ساختار `data.metrics[name].thresholds` یک شیء (Map) است (`{ 'p(95)<300': { ok: true } }`)، در حالی که کد قبلی آن را آرایه فرض می‌کرد.
  - این موضوع منجر به خطای `Object has no member 'forEach'`، عدم چاپ وضعیت PASS/FAIL آستانه‌ها و عدم تولید فایل `k6-summary.json` می‌شد. از آنجا که خطای داخل `handleSummary` کد خروج k6 را صفر نگه می‌دارد، تست‌های قبلی متوجه این شکست نمی‌شدند.
  - **اصلاح**: پیاده‌سازی تابع کمکی `thresholdRowsOf()` برای پشتیبانی همزمان از هر دو ساختار Map و Array، به‌همراه تابع `ms()` جهت قالب‌بندی امن و مقاوم در برابر پیکربندی‌های سفارشی `--summary-trend-stats`.
- **`test/run-e2e.mjs`: سخت‌سازی بررسی آرتیفکت‌های خروجی.**
  - اضافه شدن اعتبارسنجی وجود فیزیکی فایل `k6-summary.json` در تست e2e بویلرپلیت؛ تا در صورت بروز خطای بی‌صدا در `handleSummary`، اجرای تست با خطا متوقف شود.

### تغییر (Changed)

- **انگلیسی‌سازی تمام لاگ‌ها و پیام‌های ترمینال در `smoke-test.js`**:
  - برای جلوگیری از مشکلات کدگذاری کاراکترها (Encoding / Mojibake) در ترمینال ویندوز (PowerShell)، تمام پیام‌های `console.log`، لاگ‌های هشدار و گزارش‌های خروجی به زبان انگلیسی استاندارد تغییر یافتند.
- **بهینه‌سازی معماری بارگذاری فایل‌های ویدئویی**:
  - استفاده از باز کردن مستقیم به صورت باینری `open(filePath, 'b')` در فاز Init و انتقال مستقیم به `http.file` در فاز اجرا به جای راهکارهای ناسازگار `SharedArray` یا تبدیل Base64.
- **جداسازی متغیرهای محیطی از آبجکت options**:
  - استخراج متغیرهای `VUS` و `DURATION` برای رفع خطای `TypeError: Cannot read property 'vus' of undefined` در محیط Goja k6 هنگام فراخوانی در متد `setup()`.

### امنیت (Security)

- حذف توکن‌های هاردکدشده در فایل‌های تست و جایگزینی با متغیر محیطی `__ENV.API_KEY || 'MISSING-API-KEY'`.
- به‌روزرسانی `.gitignore` برای نادیده‌گرفتن فایل‌های موقت، آرتیفکت‌ها و خروجی‌های تست در پوشهٔ `test-on-fara/`.

### کشفیات فنی (Findings) — تأییدشده به‌صورت تجربی روی k6 v1.2.1

| # | کشف | اثر روی این پروژه |
|---|---|---|
| ۱ | زیرمتریک تگ‌دار در خلاصهٔ k6 **تنها زمانی تولید می‌شود که حداقل یک آستانه روی آن ترکیب تگ تعریف شده باشد** | ضرورت استفاده از `taggedSla` یا گاردهای تگ‌دار جهت دیده‌شدن متریک‌های تفکیکی در خروجی |
| ۲ | قواعد تجمیع: `Trend → avg/min/max/med/p(N)` و `Counter → count/rate` و `Rate → rate` | عدم استفاده از متدهای نامعتبر روی انواع متریک‌ها جهت جلوگیری از خروج با خطای ۱۰۴ |
| ۳ | `http_req_failed` تمامی پاسخ‌های غیر ۲xx و همچنین خطاهای لایه شبکه/تایم‌اوت را خطا می‌شمارد | آستانهٔ اصلی نرخ خطا روی همین متریک بسته می‌شود |
| ۴ | خطای داخل تابع `handleSummary` **کد خروج (Exit Code) k6 را صفر نگه می‌دارد** | ضرورت اعتبارسنجی خروجی فایل‌های آرتیفکت در اسکریپت‌های تست خودکار (`run-e2e.mjs`) |
| ۵ | موتور اجرای k6 مسیر فایل‌ها در `handleSummary` را نسبت به مسیر کاری جاری (CWD) تفسیر کرده و دایرکتوری والد ناموجود را به صورت خودکار نمی‌سازد | پوشهٔ `test-on-fara/reports/` باید از قبل وجود داشته باشد |
| ۶ | داده‌های باینری بازشده در `SharedArray` به اشیاء ساختاریافته تبدیل شده و در `http.file` خطای ناسازگاری ایجاد می‌کنند | استفاده از بارگذاری آرایه‌ای مستقیم در فاز Init (`open(..., 'b')`) |
| ۷ | خروجی کاراکترهای یونیکد/فارسی در برخی ترمینال‌های ویندوز با اشکال مواجه می‌شود | استفاده از زبان انگلیسی در پیام‌های کنسول و گزارش ترمینال |
| ۸ | بازنویسی خروجی `stdout` در `handleSummary` خروجی پیش‌فرض k6 را کاملاً لغو می‌کند | استفاده از ماژول استاندارد `textSummary` و ادغام خروجی پیش‌فرض با گزارش‌های سفارشی |

### تصمیم‌های رد‌شده (Rejected)

- **استفاده از SharedArray برای فایل‌های باینری ویدئو**: به دلیل ناسازگاری ساختاری داده‌های اشغالی در آبجکت‌های Goja با `http.file`.
- **استفاده از زبان فارسی در خروجی ترمینال (`console.log`)**: به دلیل مشکلات کدگذاری فونت و نویسه‌ها در PowerShell ویندوز.

### اعتبارسنجی (Verification)

| آزمون | نتیجه |
|---|---|
| `node test/run-e2e.mjs` | ۳۱/۳۱ تست پاس شد (`total=31 passed=31 failed=0`) شامل اعتبارسنجی آرتیفکت `base-stress-test.js` |
| `k6 inspect test-on-fara/smoke-test.js` | تأیید ساختار options، سناریوها و آستانه‌ها بدون خطا |
| اجرای واقعی `smoke-test.js` روی سرور زنده | ۳۱ تکرار کامل بدون خطای HTTP (`http_req_failed = 0%`)، قبولی تمام چک‌ها (`checks = 100%`)، زمان پاسخ `p(95) = 2543ms < 3000ms` و تولید موفق گزارش متنی در پوشه `reports/` |

### اقدامات باقی‌مانده (TODO)

- اجرای تست در بازه زمانی کامل (۵ دقیقه) مطابق سناریوی اصلی Smoke Test.
- ارزیابی و اجرای تست بار افزایشی (Stress / Safe Capacity) جهت شناسایی توان عملیاتی سرویس و دستیابی به هدف بالای ۲۰۰ درخواست در دقیقه (> 200 RPM).
- ابطال و بازتولید (Rotate) توکن تستی که پیش از این در فضای مخزن وجود داشته است.
